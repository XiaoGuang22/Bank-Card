# 用户角色权限控制说明

## 一、角色定义

系统支持三种角色，权限逐级递减：

| 角色 | 描述 | 权限范围 |
|------|------|----------|
| 管理员 | 系统最高权限用户 | 用户管理、权限分配、系统配置、所有功能访问 |
| 技术员 | 负责系统配置与字符模板维护 | 相机参数设置、字符模板制作、检测参数调整、运行检测 |
| 操作员 | 日常检测操作人员 | 选择已有模板运行检测，不可修改参数或模板 |

## 二、功能权限矩阵

| 功能模块 | 管理员 | 技术员 | 操作员 |
|---------|--------|--------|--------|
| 解决方案选择 | ✓ | ✓ | ✓ |
| 运行检测 | ✓ | ✓ | ✓ |
| 传感器设置 | ✓ | ✓ | ✗ |
| 工具配置 | ✓ | ✓ | ✗ |
| 控制设置 | ✓ | ✓ | ✗ |
| 用户管理 | ✓ | ✗ | ✗ |
| 退出登录 | ✓ | ✓ | ✓ |
| 关闭程序 | ✓ | ✓ | ✓ |

## 三、实现方式

### 3.1 UI层面控制（主要方式）

根据用户角色动态显示/隐藏功能按钮：

```python
# 管理员和技术员显示"选择或者修改"面板
if self.role in ["管理员", "技术员"]:
    lf_mod = ttk.LabelFrame(dual_pane, text="选择或者修改", style="White.TLabelframe")
    lf_mod.grid(row=0, column=1, sticky="nsew", padx=(3, 0))
    self._create_img_btn(lf_mod, "传感器", self.icons['sensor'], command=self.show_sensor_settings)
    self._create_img_btn(lf_mod, "工具", self.icons['tools'], command=self.show_tool_interface)
    self._create_img_btn(lf_mod, "控制", self.icons['control'])

# 仅管理员显示用户管理按钮
if self.role == "管理员":
    self._create_img_btn(content, "用户管理", self.icons['user'], side=tk.TOP, command=self.show_user_management)
```

### 3.2 功能入口双重检查（安全保障）

在功能方法入口添加权限检查，防止绕过UI直接调用：

```python
def show_sensor_settings(self):
    """显示传感器设置面板（管理员和技术员可访问）"""
    # 权限检查：操作员不可访问
    if self.role == "操作员":
        from tkinter import messagebox
        messagebox.showinfo("提示", "操作员无权访问传感器设置")
        return
    # ... 功能代码
```

## 四、修改文件清单

### 4.1 InspectMainWindow.py

**修改位置1：侧边栏按钮创建（约2170-2195行）**
- 根据角色控制"选择或者修改"面板显示
- 根据角色控制"用户管理"按钮显示

**修改位置2：show_user_management方法（约2261行）**
- 注释掉权限检查（UI已控制）
- 添加窗口单例管理

**修改位置3：show_sensor_settings方法（约2296行）**
- 添加操作员权限检查

**修改位置4：show_tool_interface方法（约2420行）**
- 添加操作员权限检查

### 4.2 UserManagementWindow.py

**修改内容：**
- 添加错误弹窗后自动置顶功能
- 所有messagebox调用改为自定义方法
- 修改用户时检查管理员唯一性

## 五、测试建议

### 5.1 管理员测试
- [ ] 所有按钮都可见
- [ ] 可以访问用户管理
- [ ] 可以访问传感器设置
- [ ] 可以访问工具配置
- [ ] 可以运行检测

### 5.2 技术员测试
- [ ] "用户管理"按钮不可见
- [ ] 可以访问传感器设置
- [ ] 可以访问工具配置
- [ ] 可以运行检测
- [ ] 尝试直接调用用户管理功能应被拒绝

### 5.3 操作员测试
- [ ] 只能看到"解决方案"和"运行"按钮
- [ ] "传感器"、"工具"、"控制"、"用户管理"按钮不可见
- [ ] 可以选择解决方案
- [ ] 可以运行检测
- [ ] 尝试直接调用配置功能应被拒绝

## 六、注意事项

1. **UI隐藏是主要控制手段**：不显示按钮，用户就无法点击
2. **功能入口检查是安全保障**：防止通过其他方式绕过UI调用功能
3. **用户体验优先**：操作员看不到不能用的功能，避免点击后被拒绝的挫败感
4. **管理员唯一性**：系统必须保证至少有一个管理员账号存在

## 七、未来扩展

如果需要更细粒度的权限控制，可以考虑：

1. 在配置文件中定义权限矩阵
2. 使用装饰器统一处理权限检查
3. 添加操作日志记录
4. 支持自定义角色和权限组合
